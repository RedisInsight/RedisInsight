name: 'Jest coverage report'
on:
  workflow_call:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ArtiomTr/jest-coverage-report-action@v2.3.1
        with:
          package-manager: yarn
          test-script: yarn test:cov:ui

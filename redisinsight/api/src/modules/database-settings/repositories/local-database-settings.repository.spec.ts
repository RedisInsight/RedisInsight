import { LocalDatabaseSettingsRepository } from './local-database-settings.repository';

describe('LocalDatabaseSettingsRepository', () => {
  it('should be defined', () => {
    expect(new LocalDatabaseSettingsRepository()).toBeDefined();
  });
});

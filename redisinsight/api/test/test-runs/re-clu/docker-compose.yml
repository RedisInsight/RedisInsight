version: '3.4'

services:
  test:
    env_file:
      - ./re-clu/.env
    command:
      [
        './wait-for-it.sh',
        'redis:12010',
        '-s',
        '-t',
        '120',
        '--',
        'yarn',
        'test:api:ci:cov',
      ]
  redis:
    build: ./re-clu
    cap_add:
      - sys_resource
    env_file:
      - ./re-clu/.env

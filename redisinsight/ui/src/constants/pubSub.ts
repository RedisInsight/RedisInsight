export enum SubscriptionType {
  Subscribe = 's',
  PSubscribe = 'p',
  SSubscribe = 'ss',
}

export enum PubSubEvent {
  Subscribe = 'subscribe',
  Unsubscribe = 'unsubscribe',
  Exception = 'exception',
}
